//                                      JAVASCRIPT CAPITULO 4
                         
// METODOS DE CADENAS

// let cadena = "caDena de prueba ";
// let cadena2 = "cadena"
// let numero = 22222

                                // devuelve valores booleanos para confirmar si esta o no 

// algunas devuelve valores booleanos para un funcionamiento distinto

// resultado = cadena.concat("HOLA") añade cadenas 

// resultado = cadena.startsWith(cadena2); verifica si una cadena comienza con la otra

// resultado = cadena.endsWith(cadena2) verifica si una cadena termina con palabra o letra de otra cadena

// resultado = cadena.includes(cadena2) verifica el texto que contiene otra cadena

// resultado = cadena.indexOf("prueba")  comprueba en que posicion se encuentra el string letra o palabra si da -1 no hay nada

// resultado = cadena.lastIndexOf("prueba") recorre de atras hacia adelante la funcion es parecida a la anterior


                                    // modificar y colocar texto

// resultado = cadena.padStart(20, "a") rellena la cadena de (N de espacios, "letra o palabra que queramos");

// resultado = cadena.padEnd(20,"aaaa"); mismo que el padstart pero las letras al final

// resultado = cadena.repeat(10) repite N de veces una cadena

                                    // transformacion de cadena

// resultado = cadena.split("TEXTO O ESPACIO") se transforma en array y se divide segun lo ingresado en split

// resultado = cadena.substring(N1,N2) crea un nuevo string N1 desde donde quiere que comience / N2 desde donde termina

// resultado = cadena.toLocaleLowerCase(); convierte la cadena en minuscula

// resultado = cadena.toUpperCase(); convierte la cadena en mayuscula

// resultado = numero.toString() convierte un int en string

// resultado = cadena.trim() elimina los espacios inecesarios de un string

// resultado = cadena.trimEnd() elimina los espacios del final

// resultado = cadena.trimStart() elimina los espacios del principio

// console.log(resultado);

                        // METODOS DE ARREGLOS como string pero en arreglos
                        
                        